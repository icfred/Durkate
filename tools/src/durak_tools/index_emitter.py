"""Index emit stage.

Writes the typed TS index under packages/assets/src/generated/ so the
web app references sprite paths through a typed surface. Real
implementation lands in a follow-up ticket.
"""

from __future__ import annotations


def run() -> None:
    return None
